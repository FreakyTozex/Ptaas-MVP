# Placeholder para middlewares de autenticação adicionais.
# A autenticação principal é feita via Depends() nos endpoints (core/supabase.py).
# Este ficheiro pode ser expandido para: rate limiting, IP allowlist, API key global, etc.
